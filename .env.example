# Admin authentication (use ADMIN_PASSWORD_HASH in production instead)
ADMIN_PASSWORD=mmmeisaiscute

# Generate with: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
# ADMIN_PASSWORD_HASH=

# Session signing secret (use a long random string in production)
SESSION_SECRET=pinkroom-vtuber-super-secret-2026

# Vercel KV (optional for local dev — falls back to data/site.json)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Vercel Blob (optional for local dev — falls back to public/uploads/)
# BLOB_READ_WRITE_TOKEN=
